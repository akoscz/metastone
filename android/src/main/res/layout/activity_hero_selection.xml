<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_centerVertical="true"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:background="@drawable/metastone_bg"
        tools:context="net.demilich.metastone.android.HeroSelectionActivity">

        <android.support.v4.view.ViewPager
            android:id="@+id/player1_panel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/panel"
            android:layout_weight=".5"
            android:layout_marginTop="8dp"
            android:layout_marginRight="5dp"/>

        <android.support.v4.view.ViewPager
            android:id="@+id/player2_panel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/panel"
            android:layout_weight=".5"
            android:layout_marginTop="8dp"
            android:layout_marginLeft="5dp"/>

    </LinearLayout>

    <Spinner
        android:id="@+id/deck_format"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="2dp"
        tools:listitem="@layout/metastone_spinner_dropdown_item"
        android:gravity="center_horizontal"
        android:background="@drawable/metastone_button"
        android:popupBackground="@null"
        android:spinnerMode="dropdown" />

    <ImageButton
        android:id="@+id/play_button"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_margin="8dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        tools:background="@drawable/play"/>
</RelativeLayout>
